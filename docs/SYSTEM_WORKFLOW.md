# 🔄 อธิบายขั้นตอนการทำงานระบบ Auto-Acct (System Workflow)

เอกสารนี้อธิบายกระบวนการทำงานของระบบบัญชีอัตโนมัติ ตั้งแต่การรับเอกสารจนถึงการบันทึกบัญชีเสร็จสมบูรณ์

---

## 📊 ภาพรวมการทำงาน (Overview)

ระบบถูกออกแบบมาให้ทำงานเป็น 4 ขั้นตอนหลัก:
1. **รับข้อมูล (Input)** — จาก FlowAccount หรืออัปโหลดไฟล์
2. **AI ประมวลผล (AI Processing)** — อ่านข้อมูลและลงรหัสบัญชี
3. **ตรวจสอบและอนุมัติ (Human Review)** — ทีมนักบัญชีตรวจสอบความถูกต้อง
4. **บันทึกและส่งออก (Finalize & Export)** — บันทึกเข้าระบบและส่งออกไปยัง Express

```mermaid
graph LR
    A[📄 รับเอกสาร] --> B[🤖 AI อ่าน & ลงบัญชี]
    B --> C[👥 นักบัญชีตรวจสอบ (Teable)]
    C -->|อนุมัติ| D[✅ บันทึกเข้าระบบ]
    D --> E[📤 Export เข้า Express]
```

---

## 📌 รายละเอียดแต่ละขั้นตอน

### Step 1: รับข้อมูล (Data Input)
ระบบรองรับการนำเข้าข้อมูล 2 ช่องทาง:

| ช่องทาง | รายละเอียด | สถานะ |
|---|---|---|
| **1. FlowAccount API** | ดึงเอกสารซื้อ/ขาย จากระบบ FlowAccount อัตโนมัติทุกคืน (หรือกด sync เอง) | ✅ พร้อมใช้งาน |
| **2. อัปโหลดไฟล์ (OCR)** | อัปโหลดรูปใบเสร็จ/PDF เข้า Google Drive ของบริษัท | ⚠️ กำลังพัฒนา (Phase 3B) |

> **สิ่งที่ระบบทำ:** ตรวจสอบความซ้ำซ้อน (Duplicate Check) และดึงไฟล์ภาพต้นฉบับมาเตรียมไว้

---

### Step 2: AI ประมวลผล (AI Classification)
เมื่อได้รับเอกสาร AI จะทำหน้าที่เสมือน "ผู้ช่วยลงบัญชี junior":

1. **อ่านข้อมูล (OCR):** ดึงชื่อร้านค้า, วันที่, เลขที่ใบเสร็จ, ยอดเงิน, และรายการสินค้า
2. **วิเคราะห์หมวดบัญชี:** 
   - เช่น "ซื้อกาแฟ Amazon" → ลงรหัส *5100-ค่าอาหารและเครื่องดื่ม*
   - เช่น "จ่ายค่าไฟ" → ลงรหัส *5300-ค่าสาธารณูปโภค*
3. **ประเมินความมั่นใจ (Confidence Score):**
   - ถ้ามั่นใจ > 80% → ส่งไปรออนุมัติทันที
   - ถ้ามั่นใจต่ำ (ไม่เคยเจอร้านนี้) → แจ้งเตือนให้คนช่วยดูเป็นพิเศษ

---

### Step 3: ตรวจสอบและอนุมัติ (Review & Approval)
นี่คือขั้นตอนที่ **ทีมบัญชี** จะเข้ามามีบทบาท ผ่านระบบ **Teable** (หน้าตารูปแบบตารางคล้าย Excel):

1. **Login เข้า Teable**: จะเห็นรายการที่ AI เตรียมไว้ให้ในสถานะ *"Pending Review"*
2. **ตรวจสอบความถูกต้อง**:
   - ดูรูปใบเสร็จเทียบกับข้อมูลที่ AI กรอก
   - แก้ไขรหัสบัญชี (ถ้าผิด)
3. **กดอนุมัติ (Approve)**: เปลี่ยนสถานะเป็น *"Approved"*

> **ข้อดี:** ไม่ต้องคีย์ใหม่ทั้งหมด แค่ "รีวิว" และ "แก้ไข" เฉพาะที่ผิด

---

### Step 4: บันทึกและส่งออก (Finalize & Export)
เมื่อรายการถูกอนุมัติแล้ว:

1. **บันทึกบัญชี (GL Posting):** ระบบบันทึกรายการ Dr/Cr ลงในฐานข้อมูลหลัก
2. **ส่งออกไฟล์ (CSV Export):** 
   - สามารถกด Export เป็นไฟล์ CSV เพื่อนำไป import เข้าโปรแกรม **Express** หรือโปรแกรมบัญชีอื่นๆ ได้ทันที
   - รูปแบบไฟล์รองรับ UTF-8 ภาษาไทยสมบูรณ์

---

## ⏱️ สรุปเวลาที่ประหยัดได้

| กิจกรรม | แบบเดิม (Manual) | ใช้ระบบ Auto-Acct |
|---|---|---|
| **รับเอกสาร** | รวบรวมกระดาษ, เรียงบิล | อัตโนมัติ / อัปโหลดรวมกัน |
| **อ่านและคีย์ข้อมูล** | 3-5 นาที / ใบ | **0 วินาที** (AI ทำ) |
| **ลงรหัสบัญชี** | เปิดผังบัญชีดู | **AI แนะนำให้** (แค่ตรวจทาน) |
| **ตรวจสอบ** | ต้องรื้อเอกสารมาดู | ดูรูปใบเสร็จบนหน้าจอได้เลย |
| **รวมเวลาต่อใบ** | **~5-10 นาที** | **~30 วินาที - 1 นาที** |

---

## ❓ คำถามที่พบบ่อย (FAQ) ในส่วน Flow

**Q: ถ้า AI ลงบัญชีผิด จะทำยังไง?**
A: ในขั้นตอน Step 3 นักบัญชีสามารถแก้รหัสบัญชีให้ถูกต้องได้ และระบบจะ "จำ" ไว้สอน AI ในครั้งถัดไปครับ

**Q: ต้องเข้าโปรแกรม Express ทุกวันไหม?**
A: ไม่ต้องครับ ทำงานบน Teable ให้เสร็จก่อน แล้วค่อย Export ไปเข้า Express เดือนละครั้งหรืออาทิตย์ละครั้งก็ได้

**Q: เอกสารตัวจริงต้องเก็บไหม?**
A: ตามกฎหมายยังควรเก็บเอกสารตัวจริงไว้ (หรือใช้ e-Tax Invoice) แต่การทำงานประจำวันสามารถใช้ไฟล์ดิจิทัลในระบบได้เลยครับ
